// Local-only placeholders. Replace these values with API-provided labels when the live contract is enabled.
export const localPreferences = { density: 'comfortable', defaultSite: 1, timezone: 'Asia/Kolkata', notifications: true };
export const previewNotice = 'Reserve and prospectivity signals are prioritisation aids, not confirmed deposits.';