VITE_API_BASE_URL=http://localhost:8000
# false (the default) talks to the live backend above. Set to true only as
# an offline/venue-wifi fallback when the backend isn't reachable.
VITE_USE_MOCK=false
