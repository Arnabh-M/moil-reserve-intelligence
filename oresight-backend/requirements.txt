fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
sqlalchemy>=2.0,<3.0
geoalchemy2>=0.16,<1.0
psycopg[binary]>=3.2,<4.0
alembic>=1.13,<2.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-dotenv>=1.0,<2.0
shapely>=2.0,<3.0
neo4j>=5.25,<6.0
pgvector>=0.3,<1.0  # SQLAlchemy vector column + operators for site-notes RAG search
apscheduler>=3.10,<4.0
python-multipart>=0.0.12,<1.0
pypdf>=5.1,<6.0  # PDF text extraction for POST /reports/upload
httpx>=0.27,<1.0
pytest>=8.3,<9.0

# Agent layer (app/agents/) — shortfall forecaster inference
# EXACT pin, not a range. shortfall_forecaster.pkl is only safe to load under the
# xgboost it was trained with: a pickle written by 3.4.1 and loaded by 2.1.4
# raises nothing and returns predictions up to 0.37 off (measured). Changing this
# line means retraining the model (see requirements-train.txt) and regenerating
# models/shortfall_regression_fixture.json; tests/test_model_artifact_regression.py
# fails until both are done, and app.services.forecast_model refuses to load a
# mismatched artifact.
xgboost-cpu==2.1.4
joblib>=1.4,<2.0
pandas>=2.2,<3.0
numpy>=1.26,<3.0
