# Copy this file to `.env` and adjust values for your local setup.
# All variables have sane defaults in app/config.py that match docker-compose.yml,
# so a plain `cp .env.example .env` works out of the box for local dev.

# App environment: "dev" or "prod". Only affects log verbosity / behaviour toggles.
APP_ENV=dev

# SQLAlchemy connection string for PostgreSQL + PostGIS.
# Local Docker Postgres:
# DATABASE_URL=postgresql+psycopg://oresight:oresight@localhost:5432/oresight
#
# Supabase (Shared pooler on port 5432):
# Note: Percent-encode any special characters in [YOUR-PASSWORD]
# DATABASE_URL=postgresql+psycopg://postgres.tfpikpfojxidlsedlzox:[YOUR-PASSWORD]@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres
DATABASE_URL=postgresql+psycopg://oresight:oresight@localhost:5432/oresight

# Neo4j Bolt connection details. Match the `neo4j` service in docker-compose.yml.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=oresight123

# JSON array of allowed CORS origins for the frontend dev servers.
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000","http://127.0.0.1:5173"]

# Field Intake Hardening Phase 1 — hours after a production record's
# creation during which PATCH /production/{id} is allowed. Edits attempted
# after this window return 403. A supervisor-role bypass is planned for a
# future auth phase; not implemented yet, so this is a hard cutoff for now.
PRODUCTION_EDIT_WINDOW_HOURS=48

# Field Intake Hardening Phase 4 — flap detection (§2.2). A status change
# that pushes an equipment's log-row count within the trailing window above
# the threshold opens one `equipment_flapping` risk event (deduped like
# `equipment_failure` — no duplicate open event).
EQUIPMENT_FLAP_WINDOW_HOURS=24
EQUIPMENT_FLAP_THRESHOLD=4
