scikit-learn
xgboost-cpu==2.1.4  # same distribution AND version as oresight-backend/requirements.txt: model pickles are not portable across versions, and `xgboost` + `xgboost-cpu` overwrite each other's files if both are installed
pykrige
geopandas
rasterio
shapely
pyproj
scipy
joblib
neo4j
pandas
numpy
jupyter
jenkspy>=0.4
